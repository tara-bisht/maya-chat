# `apps/web`

Next.js 15 App Router. Maya Street Cast tokens live in `app/globals.css`. Marketing lobby is `/`; auth stub is `/login`.

Chat, gallery, Studio, and `/api/*` arrive in later PRs. See [`../../docs/implementation-plan.md`](../../docs/implementation-plan.md).
