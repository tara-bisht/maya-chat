export function lockedAgentCopy(shortName: string): string {
  return `${shortName} is on the Plus bill. Marcus and Dr. Priya are already in.`;
}

export function quotaCopy(limit: number): string {
  return `Daily curtain. ${limit} lines. Come back tomorrow, or buy a better seat.`;
}
